1) Have a function wrapper that returns a hamiltonian matrix based on param (dictionry with parameters)
    -> JS: create a TB hamitlonian matrix without magnetic term "generate_H_TB(...)"-> np.array
    -> MB: funnction miniminiznig the energy of the system H_TB+H_SOC (np.linalg.eigvalsh(H_TB+H_SOC))
    -> For  now MB focus on miniminizng H_SOC alone. 
    -> H_SOC from generate H_SOC and use tests/test_cases/params_2_atoms


     